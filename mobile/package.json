{
    "name": "one2one-chat-mobile",
    "private": true,
    "version": "1.0.0",
    "description": "Capacitor Android/iOS shell for One2One Chat",
    "scripts": {
        "sync": "cap sync android",
        "open": "cap open android",
        "apk:debug": "cap sync android && cd android && gradlew assembleDebug",
        "apk:release": "cap sync android && cd android && gradlew assembleRelease"
    },
    "dependencies": {
        "@capacitor/android": "^8.5.2",
        "@capacitor/app": "^8.1.1",
        "@capacitor/core": "^8.5.2"
    },
    "devDependencies": {
        "@capacitor/cli": "^8.5.2"
    }
}
