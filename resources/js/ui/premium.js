/**
 * Settings › Premium (Y2). Implemented by the plans slice: plan cards, the current subscription,
 * the payment sheet and the verified-badge card.
 */
export function initPremium(config) {
    return { config };
}
