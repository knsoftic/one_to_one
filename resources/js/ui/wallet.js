/**
 * Settings › Wallet (Y2). Implemented by the wallet slice: balance, earned/purchased tiles, coin
 * packs, pending payments, history and the "Coming soon" withdraw button.
 */
export function initWallet(config) {
    return { config };
}
